@model IEnumerable<Datos.Datos.Disco>

@{
    ViewBag.Title = "Index";
    string madre = "";
    if (ViewBag.Message != null && ViewBag.Message != "")
    {
        madre = ViewBag.Message;
    }
}


@*<h2>Index</h2>

    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.imagen)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Autor.nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Categoria.nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Discografica.nombre)
            </th>
            <th></th>
        </tr>

    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.imagen)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Autor.nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Categoria.nombre)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Discografica.nombre)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.id }) |
                @Html.ActionLink("Details", "Details", new { id=item.id }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.id })
            </td>
        </tr>
    }

    </table>*@



<!-- Header Start -->
<div class="jumbotron jumbotron-fluid mb-5">
    <div class="container text-center py-5">
        <h1 class="text-white display-3">Discos</h1>
        <div class="d-inline-flex align-items-center text-white">
            <p class="m-0"><a class="text-white" href="">Incio</a></p>
            <i class="fa fa-circle px-3"></i>
            <p class="m-0">Discos</p>
        </div>
    </div>
</div>
<!-- Header End -->


<h2>@madre</h2>


<!-- Discos -->
<div class="container-fluid pt-5">
    <div class="container">
        <div class="text-center pb-2">
            <h6 class="text-primary text-uppercase font-weight-bold">La mejor música</h6>
            <h1 class="mb-4">Lista Discos</h1>
            <p class="btn btn-primary py-2 px-4 my-2">@Html.ActionLink("Añadir disco", "Create")</p>

        </div>



        <div class="row">
            @foreach (var item in Model)
            {
                <div class="col-md-4 mb-5">
                    <div class="bg-secondary">
                        <div class="text-center p-4">
                            <img style="height: 300px; object-fit: cover; " class="disco-img" src="@Html.DisplayFor(modelItem => item.imagen)" alt="">
                        </div>
                        <div class="bg-primary text-center p-4">
                            <h3 class="m-0">@Html.DisplayFor(modelItem => item.nombre)</h3>
                        </div>
                        <div class="d-flex flex-column align-items-center py-4">
                            <h5>@Html.DisplayFor(modelItem => item.Autor.nombre)</h5>
                            @* <p>@Html.DisplayFor(modelItem => item.Categoria.nombre)</p>*@
                            @*<a href="Temas.html" class="btn btn-primary py-2 px-4 my-2">Canciones</a>*@
                            <p >@Html.ActionLink("Canciones del disco", "Index", "Tema", new { id = item.id, madre = "Disco", nombreMadre = Model.FirstOrDefault().nombre }, null)</p>
                            <p class="btn btn-primary py-2 px-4 my-2">@Html.ActionLink("Detalles", "Details", new { id = item.id })</p>

                        </div>
                    </div>
                </div>

            }
        </div>

    </div>

</div>
<!-- Discos final -->
<!-- Paginacion inicio -->
<!--<div class="row">
    <div class="col-12">
        <nav aria-label="Page navigation">
            <ul class="pagination pagination-lg justify-content-center mb-0">
                <li class="page-item disabled">
                    <a class="page-link" href="#" aria-label="Previous">
                        <span aria-hidden="true">&laquo;</span>
                        <span class="sr-only">Anterior</span>
                    </a>
                </li>
                <li class="page-item active"><a class="page-link" href="#">1</a></li>
                <li class="page-item"><a class="page-link" href="#">2</a></li>
                <li class="page-item"><a class="page-link" href="#">3</a></li>
                <li class="page-item">
                    <a class="page-link" href="#" aria-label="Next">
                        <span aria-hidden="true">&raquo;</span>
                        <span class="sr-only">Siguiente</span>
                    </a>
                </li>
            </ul>
        </nav>
    </div>
</div>-->
<!-- Paginacion final -->
